databases:
  - name: libreria-db
    plan: free

services:
  - type: web
    name: tp-libreria-paw
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: libreria-db
          property: connectionString
